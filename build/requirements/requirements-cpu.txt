# =============================================================================
# Audio Separator + Apollo - CPU Requirements
# =============================================================================
# Install with: uv pip install -r requirements-cpu.txt
# Or: pip install -r requirements-cpu.txt
# =============================================================================

# PyTorch CPU 2.10.0 - For consistency with working builds
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
torchaudio==2.10.0+cpu
torchvision==0.25.0+cpu

# Numpy pinned for audio-separator 0.41.0 (requires numpy>=2)
numpy>=2.0.0

# Core audio processing
audio-separator==0.41.0
librosa>=0.10.0
soundfile>=0.12.0
samplerate==0.1.0  # audio-separator requires exactly this version
pydub>=0.25.0
scipy>=1.10.0
resampy>=0.4.0

# ONNX (CPU version)
onnx>=1.14.0
onnx2torch>=1.5.0
onnxruntime>=1.16.0

# Model dependencies - match audio-separator requirements
einops>=0.7.0
julius>=0.2.7
diffq>=0.2.4  # audio-separator requires diffq
beartype>=0.18.5,<0.19.0  # audio-separator requires this version range
rotary-embedding-torch>=0.6.1,<0.7.0  # audio-separator requires this version range

# Utilities
requests>=2.28.0
tqdm>=4.65.0
six>=1.16.0
pyyaml>=6.0
omegaconf>=2.3.0
huggingface-hub>=0.20.0
pytorch-lightning>=2.0.0
ml-collections>=0.1.0

# Build tools - pinned to working version
cx-Freeze==6.15.16
